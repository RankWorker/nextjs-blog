# Changesets

Run `npm run changeset` for every user-visible change. Patch releases fix bugs,
minor releases add backward-compatible behavior, and major releases change the
public API or stable styling hooks.
